<UserControl x:Class="SolidSaverWPF.PropertyRename.ChangePropertyField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SolidSaverWPF.PropertyRename"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300">
    <Grid ShowGridLines="True" Height="20" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="1" Text=">>" HorizontalAlignment="Center"/>
        <TextBlock 
            x:Name="PropConfigName"
            Text="{Binding Key}"
            Grid.Column="0"/>
        <TextBox
            x:Name="PropOldvalue"
            Text="TestText"
            IsReadOnly="True"
            Foreground="Gray"
            Grid.Column="2"/>

        <TextBox 
            x:Name="PropConfigvalue"
            Background="Transparent"
            Grid.Column="2"/>

    </Grid>
</UserControl>
