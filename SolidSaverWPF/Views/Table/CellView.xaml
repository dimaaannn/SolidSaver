<UserControl x:Class="SolidSaverWPF.Views.Table.CellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SolidSaverWPF.Views.Table"
             xmlns:tablemodel="clr-namespace:SolidSaverWPF.ViewModel.Table"
             d:DataContext="{d:DesignInstance Type=tablemodel:CellViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="150">

    <UserControl.DataContext>
        <tablemodel:CellViewModel></tablemodel:CellViewModel>
    </UserControl.DataContext>
    
        <!--d:DataContext="{d:DesignInstance Type=tablemodel:CellViewModel, IsDesignTimeCreatable=True}"-->
    <Grid
        Background="{Binding BGColorBrush}">
        <StackPanel>
            <TextBlock Text="{Binding Text, FallbackValue=test}"/>
            <TextBox Text="{Binding TempText, FallbackValue=test}"/>
        </StackPanel>

    </Grid>
</UserControl>
